$(document).ready(function(){

    $('.cars_slider').slick({
    	// console.log(direction);
        dots: true,
 	 	// infinite: false,
		// speed: 300,
		slidesToShow: 1,
		slidesToScroll: 1
      });
});